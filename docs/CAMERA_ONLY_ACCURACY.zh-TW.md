# ARKit 與無 LiDAR 掃描品質改進

[English](CAMERA_ONLY_ACCURACY.md) | **繁體中文**

## 問題與改動

原先無 LiDAR 路徑在深度缺席時省略平移模糊項，並每秒把整份 rawFeaturePoints 直接當成灰色點寫入 voxel。同一特徵的反覆估計會重複投票，初期不穩定位置也可能留下多個點；旋轉觸發快門則會累積缺少基線的影格。

現在以四層品質檢查改善：

1. `PoseContinuityGate` 檢查每幀姿態；位移超過 max(8 cm, 3 m/s × dt)、旋轉超過 max(15°, 4 rad/s × dt)，或影格間隔超過 0.25 秒便重新等待追蹤連續正常 0.6 秒。這是資料接收防護，不會修改 ARKit 內部姿態。
2. `CameraOnlyGeometry` 使用 ARKit／OpenGL 相機的 -Z 前方投影，拒絕背後、越界、距離無效與非有限特徵。主執行緒最多取樣 512 點；畫面中央至少 6 點時使用深度的近側四分位數。深度不足則按 0.5 m 估算平移模糊，避免將未知距離解讀為零模糊。估計不會顯示為 LiDAR 距離警告。
3. RGB 模式需至少 12 個可見取樣特徵、覆蓋 3×3 網格中的至少 3 格；近距離抓幀位移取 min(5 cm, max(4 cm, 估計深度 × 0.05))，旋轉觸發亦需 4 cm 平移。第一張仍在品質通過後建立基準。最短拍攝間隔為 0.10 秒；LiDAR 近距離可加密至至少 2 cm，兩種模式皆維持品質和寫入背壓。
4. `SparseLandmarkFilter` 每 0.2 秒觀測特徵 ID，至少 3 次、位置變化在 2 cm + 距離 × 1.5% 內、基線至少 4 cm、視差至少 1.5° 才收錄。隔超過 2 秒或位置突變會重新累積候選。追蹤中斷／突跳會變更 epoch，不混用之前的候選。同一 ID 融入一次，採用通過驗證時 ARKit 最新位置，不對不同時刻的估計反覆加權。

稀疏點在背景 actor 中投影並從自有相機影像副本取色，仍交由 ARAnchor 局部空間磚處理後續座標修正。候選上限 20,000 個；已收錄 ID 上限與 `maxPoints` 一致（目前 600,000），達上限後停止接納新 ID 但照片拍攝仍可繼續。過期候選清理限流，避免滿容量時每個新 ID 都掃過完整字典。

## 使用方式與取捨

關閉 LiDAR 後，對準有紋理的表面緩慢側向移動，避免只在原地轉手機。近距離需要更慢的平移，以免模糊超標。HUD 會提醒紋理不足與側向移動；RGB 不顯示依 LiDAR 重複觀測設計的融合熱圖／完成比例。

點數可能比舊版少，尤其白牆、反光表面、純旋轉及短掃描。若停止時沒有通過驗證的點，會提醒側向補拍，已保存照片仍可查看。門檻是初始保守值，集中於 `CaptureConfig`，尚未以真機資料校準。

以上即時稀疏點檢查本身不是獨立 RGB 特徵匹配／三角化，也不會新增 LiDAR 深度或啟用依賴深度的 BA。ARKit 特徵點的系統性誤差仍可能通過檢查；同 ID 僅收錄一次，也代表後續精細位置估計不會逐點取代已收錄資料，只套用空間磚錨點修正。停止後另執行以下獨立影像重建；它仍採固定 ARKit 姿態，無法消除系統性位姿誤差。

## 無深度的姿態精修

相機模式掃描現在也有光束法平差。它和 LiDAR 精修一樣，在關鍵幀錨點回讀之後、模糊複核與影像重建之前執行。`CameraOnlyPoseRefinement` 由兩部分組成：純影像特徵追蹤，以及以 ARKit 逐幀運動為先驗的聯合 BA。可用 `CaptureConfig` 的 `cameraOnlyPoseRefinement` 關閉。

### 只用影像的特徵追蹤（`CameraOnlyTracker`）

- **偵測：** 每個關鍵幀以長邊 640 px 解碼。16×12 的格子中，沒有追蹤中特徵的格子最多新增一個 Shi-Tomasi 角點；同時追蹤最多 192 條。
- **追蹤：** 以金字塔 Lucas-Kanade（3 層、15×15 視窗、平均值正規化以容忍曝光變化）追到下一幀。搜尋起點是輸入姿態在目前場景深度估計下預測的位置。
- **檢查：** 以下條件都成立才保留：
  - 反向追蹤回到 0.7 px 內；
  - patch 相關係數至少 0.8；
  - 與輸入姿態的極線距離在 4 px 內（原始解析度）。

  極線檢查看不到沿移動方向的誤差。
- **track：** 超過 40 幀就結束，短於 3 幀的捨棄；讀不到的影格會結束所有 track。
- **輸出：** 觀測不帶深度。場景深度估計依 track ID 排序，重複執行結果完全相同。

### 光束法平差

`BundleAdjuster` 把 `depth <= 0` 視為沒有深度量測：略過深度殘差，改以視線三角化 track（視差至少 1°）。相機模式的 track 再以重投影精修每個點（`optimizeTracks`）。

其餘沿用 LiDAR 預設：
- 全部影格聯合求解；
- ARKit 相對運動為緊的先驗（每步 0.3 mm、0.01°）；
- 對輸入姿態的弱錨定與 30 次迭代。

LiDAR 觀測一定帶深度，所以 LiDAR 路徑不變。

**驗證：** 每 5 條 track 保留 1 條不參與求解；保留集重投影中位數至少改善 3% 才套用。沒有照片對齊檢查，因為它需要深度。

**報告：** `camera-pose-refinement.json` 記錄：
- 狀態：`applied`、`holdoutDidNotImprove`、`insufficientHoldoutTracks`、`insufficientTrackSupport`、`noImprovement`、`noObservations`、`insufficientFrames` 或 `cancelled`；
- 追蹤統計；
- 保留集修正前後的中位數；
- 修改的影格數、修正量中位數與最大值，以及耗時。

掃描摘要會像 LiDAR 掃描一樣顯示保留集的變化。

### 兩份重播掃描的量測

`tools/refine_camera_only.swift` 對 LiDAR 掃描的模擬相機模式拍攝（移除深度）執行 App 的 `CameraOnlyPoseRefinement`，結果交給 `replay_camera_only --candidate` 評分。LiDAR 參考不是真值。

| 相機模式姿態：精修前 → 精修後 | 7F2187 | 9F8040 |
| --- | --- | --- |
| track 數；長度中位數 | 3,033；5 幀 | 4,367；7 幀 |
| 保留集重投影中位數 | 1.74 → 1.05 px（−40%） | 1.83 → 1.14 px（−38%） |
| 修改影格；修正量中位數／最大 | 194；4.0／11.2 mm | 372；10.3／35.4 mm |
| 與參考的位置差中位數 | 1.0 → 0.81 cm | 1.8 → 1.32 cm |
| 5 m RPE 中位數 | 1.8 → 1.40 cm | 2.7 → 2.12 cm |
| 照片 NCC 相對輸入的改善：相鄰；寬基線（參考姿態） | +0.0078；+0.0033（+0.0063；+0.0227） | +0.0023；+0.0187（+0.0030；+0.0211） |
| MVS 點數 | 14,474 → 18,875 | 11,365 → 18,274 |
| MVS 準確度中位數／P90 | 1.95／8.78 → 1.97／7.21 cm | 2.95／9.28 → 2.39／6.58 cm |
| MVS 超過 10 cm 的點 | 7.7 → 4.3% | 8.2 → 3.9% |
| MVS 5 cm 完整度 | 23.7 → 28.9% | 10.1 → 14.3% |
| Mac 耗時（追蹤＋求解） | 4.1 + 0.6 s | 7.4 + 1.2 s |

- **房間尺度：** 精修補回 LiDAR 參考姿態大部分的寬基線照片對齊，MVS 離群點減半。
- **近距離：** 相鄰對齊與離群點改善，但寬基線對齊接近輸入。它的 track 很短（中位數 5 幀），相距較遠的視角幾乎沒被連起來。

### 預設值的選擇

兩份掃描上的各種變化（5 cm 完整度／超過 10 cm 的點／寬基線照片改善）都很接近：

| 變化 | 7F2187 | 9F8040 |
| --- | --- | --- |
| **預設**：三角化後再精修點 | 28.9%／4.3%／+0.0033 | 14.3%／3.9%／+0.0187 |
| 只三角化 | 28.3%／4.6%／−0.0005 | 15.2%／3.9%／+0.0210 |
| 較密的 24×18 格、400 條 track | 28.4%／5.4%／−0.0009 | 15.7%／4.0%／+0.0242 |
| 較鬆的運動先驗（1 mm、0.03°） | 28.2%／4.5%／+0.0015 | 15.1%／4.2%／+0.0137 |
| 較緊的運動先驗（0.1 mm、0.003°） | 27.4%／5.0%／+0.0023 | 14.8%／4.1%／+0.0160 |
| track 最長 80 幀 | 28.6%／4.7%／+0.0017 | 15.3%／3.7%／+0.0207 |
| 較寬鬆的檢查（反向 1 px、極線 6 px） | 28.1%／4.8%／+0.0014 | 15.1%／3.8%／+0.0224 |

預設以外的變化，是在場景深度估計改為可重現之前跑的；當時同一設定重跑約有半個百分點的差異，所以這些差距都在雜訊範圍內。預設採用精修點，是因為它在近距離的準確度與離群點最好，且兩份掃描的寬基線改善都為正。

### 限制

- **慢速漂移修不掉：**
  - 沒有迴環或深度時，比 track 長度還慢的漂移會被三角化出的結構吸收。合成測試中，30 幀內平滑的 2 cm 漂移仍留著。
  - ARKit 的緊先驗也會保留大部分逐幀抖動：合成測試的保留集重投影降了 45%，但 4 幀位移誤差只由 9.0 降到 8.2 mm。
  - 真實掃描的改善，來自特徵 track 看得到的多視角一致性。
- **追蹤限制：** 重複紋理、動態模糊與低紋理會限制追蹤；極線檢查看不到沿移動方向的誤差。
- **尚未實測手機：** 耗時與記憶體都是 Mac 數字，手機可能慢數倍；證據只有同一台裝置的兩份掃描。

## 停止後純 RGB 多視角重建

### 操作與資料流

在開拍前關閉 LiDAR，「影像深度重建」預設開啟，可關閉來比較只用稀疏點的結果。此選項在開始掃描時固定，續掃沿用。停止後先完成錨點姿態校正與模糊複核，再以 `RGBReconstructionEngine` 在背景讀取照片。僅 `.keep` 影格可參與，`.drop` 和 `.demote` 都排除。

`RGBReconstructionEngine` 執行已知姿態的 PatchMatch 多視角立體（MVS）。先前版本是取樣式的雙來源匹配器、使用正對相機的 patch；比較見[量測結果](#兩次-iphone-17-pro-掃描的量測結果)。

### 視角與來源

1. **視角：** 沿可用影格序列均勻選最多 48 個（`rgbMaxReferenceFrames`）；拍攝快門本來就依移動量間隔取幀。每張只解碼一次，長邊縮到 320 px（`rgbMaxImageDimension`，不放大小圖），同步縮放內參並保持 sensor 原方向；尺寸與內參不符就跳過。解碼平行進行。
2. **來源：** 每個視角同時是參考，也是其他視角的來源。每個視角最多選 4 個來源（`rgbSourceViews`），只看幾何、不做影像檢索：
   - 候選須相距至少 4 cm、視線方向相差 45° 以內；
   - 以參考視角的 36 個取樣點評分（4×3 像素格，深度 0.7、1.4、2.8 m），候選看得到的點依三角化角度計分：1° 到 5° 間逐漸提高，5°–25° 滿分，45° 降為 0；
   - 與已選來源相距 2 cm 內的近乎重複位置會跳過，讓來源涵蓋不同基線。

   來源少於兩個的視角不產生深度。

### 深度圖（PatchMatch）

每隔兩個像素（`rgbPixelStride`）估計深度與表面法向量。
- **紋理門檻：** 5×5 取樣（取樣間隔 2 px，涵蓋 9×9 px）的亮度標準差至少 0.012（`rgbMinPatchStd`），且兩個方向都有梯度。白牆與單一條紋不產生深度。
- **成本：** patch 依假設平面（單應性）變形到各來源，因此斜看的地板與牆面也能匹配。每個來源以雙邊權重的零均值正規化互相關（ZNCC）評分，權重避免前景邊緣把背景一起拉過來。成本取最好一半來源的平均，一個被遮擋的來源不會否決可見表面。
- **搜尋：** 假設值在 0.25–5 m 內隨機初始化，法向量與視線夾角在 75° 內。四輪掃描交替方向；每個像素先試已走過鄰居的平面（傳播），再試粗、細深度步長、法向擾動與兩者組合，步長每輪減半。每個視角有自己的亂數種子，結果不受執行緒排程影響。
- **照片接受：**
  - 聚合成本不超過 0.35（`rgbMaxMatchCost`）。
  - 至少兩個來源各自匹配且視差至少 1.5°。
  - 唯一性：沿最佳來源的極線，在匹配點 ±16 px 內（排除 ±2 px），其他深度的成本不得在 0.05 以內（`rgbUniquenessMargin`）。磁磚、格柵等重複紋理因此被排除，否則會產生分層的鬼影表面。

### 多視角一致性與融合

- **相鄰深度圖：** 至少兩張相鄰深度圖（`rgbConsistentViews`）要獨立得到同一表面，才保留該深度。相鄰指該視角的來源，以及把它當作來源的視角。
- **檢查方式：** 把點投影到相鄰視角，讀取該處深度再投影回來，偏移需在 stride + 1 px 內、深度差在 1% 內（`rgbConsistencyDepthRatio`）。
- **融合：**
  - 通過的點取各一致估計的平均，採參考影像顏色，依匹配品質、一致視角數與距離加權。
  - 逐視角插入 2 cm voxel。RGB 表面附近一格內不混入稀疏點，其他區域保留已驗證稀疏點；最終仍受 `exportMaxPoints` 限制。

### 輸出、報告與成本

影像重建結果直接進入 `review.ply`、歷史預覽、同步照片路線、COLMAP／PLY 匯出與 3DGS 初始化；不建立假 LiDAR 深度檔，不啟用依賴實測深度的 BA。刪除歷史仍刪除整個掃描資料夾，包含照片、模型及報告。

`capture-meta.json` 有可選的 `rgbReconstructionEnabled`（舊檔無此欄位仍可讀）。`rgb-reconstruction.json`（第 2 版，方法 `known-pose-patchmatch-mvs`）保存：
- 可用影格與解碼影像數；
- 嘗試與實際產生點的視角數及其 ID；
- 像素漏斗：有紋理、照片一致、模稜兩可（被唯一性排除）、多視角一致；
- 觀測數、RGB 點數與含稀疏點的總數；
- 全部 MVS 設定與耗時。

狀態包含 `insufficientViews`、`insufficientBaseline`、`noReliableMatches`、`reconstructed`。關閉掃描畫面時，進行中的重建會像 LiDAR 融合一樣被取消。

記憶體受視角數上限限制：48 個 320×240 視角約佔 29 MB RGBA 與灰階影像、約 7 MB 深度與成本圖；每個工作執行緒在匹配時另有一張 0.3 MB 法向量圖。在 8 核心 Mac 上，這兩份掃描每次約 2–3 秒。

### 限制

這是 320 px、固定姿態的 MVS，沒有 SfM 或 RGB 光束法平差，姿態誤差會直接帶進點雲（見下方參考姿態與相機模式姿態的比較）。超過 48 個視角上限的影格不參與匹配，大場景仍有空缺。低紋理表面（素面牆、天花板）、反光、動態模糊與遮擋會留下破洞；唯一性檢查也會排除部分細緻規律紋理上的正確匹配。手機耗時、熱量與真實精度仍需實機量測。

## 桌機以 LiDAR 為參考重播

### 目的

`tools/replay_camera_only.swift` 用真實 LiDAR 掃描量測相機模式（關閉 LiDAR）的結果。工具重播相機模式流程並忽略已存的深度，再以 LiDAR 資料作為參考。基準姿態是 ARKit 姿態加關鍵幀錨點回讀，也就是[相機模式精修](#無深度的姿態精修)之前的姿態。沒有 LiDAR 時不執行迴環修正與依賴深度的照片對齊檢查。點雲來自固定姿態 MVS。`--candidate` 可接受任意姿態檔；相機模式精修就是用 `tools/refine_camera_only.swift` 產生的姿態以這種方式評分。

1. **模擬拍攝。** 基準姿態（預設 `review-poses.jsonl`，即 ARKit + 錨點）依時間戳排序。某幀與上一個保留幀相距至少 `cameraOnlyMinBaselineM`（4 cm）且晚至少 0.10 s 才保留。接著移除深度欄位，並以候選姿態執行 `BlurFilter.annotate`，與 App 在 MVS 前的做法相同。`--all-frames` 會略過抽幀。
2. **姿態**在模擬後的影格上與參考比較。報告列出同一世界座標下的原始位置與旋轉差、以相機中心做最佳剛體對齊（Horn 四元數法）後的 ATE、Umeyama Sim(3) 尺度（大於 1 代表候選路徑較大），以及沿參考路徑 1 m 與 5 m 的相對姿態誤差（RPE）。
3. **照片對齊**使用 LiDAR 深度，與相機模式流程無關。`PhotometricPoseValidator` 在同一批有深度的影格上，比較相機模式姿態與參考姿態（以及候選姿態）。
4. **LiDAR 參考點雲。** 以參考姿態對全部影格執行預設融合濾波。容量提高到 2,000,000 點與 512 MB 工作集，避免手機的 250,000 點與 96 MB 上限讓參考點雲變粗。
5. **MVS**（`RGBReconstructionEngine`）在無深度影格上執行兩次：用參考姿態可單獨評估 MVS 品質，用候選姿態則量測端到端結果。兩次使用相同的模糊判定。
   - 準確度：每個 MVS 點到最近 LiDAR 點的距離，上限 10 cm。
   - 完整度：LiDAR 點中，2、5、10 cm 內有 MVS 點的比例。
   - 另外只計算該次實際產生點的參考視角「看得到」的 LiDAR 點。點必須落在深度圖內、相機深度介於 0.25–5 m，且與該幀 LiDAR 深度相差不超過 max(3 cm, 3%)。整體偏低而可見部分偏高，代表參考視角太少；兩者都偏低，代表每個視角產生的點太少。

參考姿態透過 `replay_pose_refinement` 取得，即 App 的 LiDAR BA 與照片對齊檢查；建置指令見[姿態精修](POSE_REFINEMENT.zh-TW.md)。

```sh
/tmp/replay_pose_refinement refine SCAN /tmp/ref.jsonl --no-surface
swiftc -O -module-cache-path /tmp/fable-swift-cache \
  arkit-3dgs-scanner/Capture/{Localization,Models,BlurFilter,CaptureConfig,DepthSampleFilter,RefusionEngine,SurfaceTSDF,TrainingFrameSelector,PhotometricPoseValidator,RGBStereoMatcher,RGBReconstructionEngine}.swift \
  tools/camera_only_metrics.swift tools/replay_camera_only.swift -o /tmp/replay_camera_only
/tmp/replay_camera_only SCAN WORK_DIR --reference /tmp/ref.jsonl \
  [--baseline FILE] [--candidate FILE] [--all-frames] [--no-mvs] [--reference-voxel M]
```

工具只讀取掃描資料夾，不會修改。`WORK_DIR` 不能已存在，也必須位於掃描資料夾之外。輸出包含 `camera-only-replay.json`（報告版本 1，含耗時）、`reference.ply`、`mvs-reference-poses.ply`、`mvs-candidate-poses.ply` 與 `refusion-progress.json`。工作資料夾會暫時以硬連結指向掃描的照片與深度，結束時移除。`--no-mvs` 略過參考點雲與 MVS。`--reference-voxel` 是診斷用選項，只改變參考點雲的融合 voxel。

### 兩次 iPhone 17 Pro 掃描的量測結果

以下使用預設設定。參考姿態由 `replay_pose_refinement refine --no-surface` 產生，兩次掃描都通過其照片對齊檢查。相機模式姿態為 `review-poses.jsonl`（ARKit + 錨點；兩次掃描在此檔都沒有套用 BA 修正）。所有數值都是與 LiDAR 參考的差異，不是相對於真值的誤差。MVS 各列先列參考姿態、再列相機模式姿態，以「vs」分隔（每次只有一個數值時用「/」）。照片 NCC 為前 → 後的中位數，括號內是逐對變化的中位數。

| | 7F2187（近距離、快速移動） | 9F8040（房間尺度） |
| --- | --- | --- |
| 已存 → 模擬相機模式 → 可用於 MVS 的影格 | 399 → 277（46 s）→ 194（模糊複核排除 83） | 569 → 372（113 s）→ 261（降級 111） |
| 原始位置差 中位數／P95／最大 | 1.0／3.0／3.7 cm | 1.8／2.9／3.8 cm |
| 原始旋轉差 中位數／P95／最大 | 0.34／0.72／0.81° | 0.31／0.65／1.38° |
| 對齊後 ATE RMSE／中位數；對齊後旋轉中位數 | 1.3／0.8 cm；0.40° | 1.4／1.1 cm；0.38° |
| Sim(3) 尺度（相機模式／參考） | 1.0002 | 1.0027 |
| RPE 1 m：平移中位數／P90；旋轉中位數 | 1.2／2.4 cm（1.25／2.40%）；0.38° | 1.4／2.2 cm（1.41／2.19%）；0.37° |
| RPE 5 m：平移中位數／P90；旋轉中位數 | 1.8／3.3 cm（0.35／0.66%）；0.42° | 2.7／4.5 cm（0.54／0.89%）；0.49° |
| 照片 NCC，相機模式 → 參考姿態：相鄰；寬基線 | 0.964 → 0.978（+0.006）；0.848 → 0.895（+0.023） | 0.988 → 0.990（+0.003）；0.885 → 0.923（+0.021） |
| 這批影格的照片檢查結果 | 通過 | 未通過（80 對中 15 對樣本流失過多；上限 12 對） |
| LiDAR 參考點雲；點間距中位數 | 162,014 點；1.32 cm | 620,246 點；1.36 cm |
| MVS 點數；48 個視角中實際產生點者 | 19,076／14,474；48／45 | 20,770／11,365；45／46 |
| MVS 準確度 中位數／P90 | 1.16／4.18 cm vs 1.95／8.78 cm | 1.34／5.27 cm vs 2.95／9.28 cm |
| MVS 點在 2 cm 內；超過 10 cm | 73.6%、2.0% vs 50.9%、7.7% | 65.0%、1.6% vs 39.0%、8.2% |
| 完整度 2／5／10 cm，全部 LiDAR 點 | 15.0／36.9／56.6% vs 7.7／23.7／42.0% | 4.6／17.3／32.5% vs 1.6／10.1／25.8% |
| 完整度 2／5／10 cm，僅可見點 | 15.1／37.2／56.9% vs 7.8／24.2／42.7% | 4.7／17.5／32.8% vs 1.6／10.2／26.0% |
| 實際產生點的視角看得到的 LiDAR 點比例 | 98.9%／98.0% | 98.5%／98.7% |
| Mac 重播耗時（8 核心）：總計；融合；照片；每次 MVS | 約 12 s；2.7 s；2.0 s；2.9–3.0 s | 約 16 s；6.7 s；2.0 s；2.1 s |

**姿態。** 相機模式姿態與 LiDAR 參考的差異中位數為 1–2 cm，最大約 4 cm，旋轉中位數約 0.3°。每移動 1 m 的差異為 1.2–1.4 cm；5 m 內只有 1.8–2.7 cm，因此在這兩條 16–20 m 的路徑上，差異沒有隨距離穩定累積。兩次掃描改用參考姿態後照片對齊都變好，主要是寬基線影像對。9F8040 的中位數雖有改善，驗證器的樣本保留規則仍判定這批子集「未通過」；完整 569 幀的精修則通過該檢查。

**點雲。** 先前取樣式的雙來源匹配器是主要瓶頸：近距離約 1,000 點、房間尺度約 200 點，5 cm 內完整度最多 12%，雖然它的視角已看得到九成以上的表面。PatchMatch 讓點數增加 15–95 倍；5 cm 完整度在近距離約提高 3 倍，在房間尺度提高 15–25 倍：

| 參考姿態／相機模式姿態 | 7F2187 先前 | 7F2187 PatchMatch | 9F8040 先前 | 9F8040 PatchMatch |
| --- | --- | --- | --- | --- |
| 點數 | 1,129／995 | 19,076／14,474 | 218／216 | 20,770／11,365 |
| 準確度中位數 | 1.25／2.56 cm | 1.16／1.95 cm | 2.48／3.10 cm | 1.34／2.95 cm |
| 準確度 P90 | 4.44／7.49 cm | 4.18／8.78 cm | 7.20／≥10 cm | 5.27／9.28 cm |
| 超過 10 cm 的點 | 2.4／4.8% | 2.0／7.7% | 4.6／11.6% | 1.6／8.2% |
| 5 cm 內完整度 | 11.6／7.6% | 36.9／23.7% | 0.7／0.6% | 17.3／10.1% |
| 每次 MVS 耗時（Mac） | 0.9 s | 2.9–3.0 s | 0.6 s | 2.1 s |

- **參考姿態：** 可單獨評估 MVS，所有準確度指標都變好。
- **相機模式姿態：** 姿態差異成為主因。中位數變好；但近距離超過 10 cm 的比例由 4.8% 升到 7.7%，因為多出的大量點來自姿態誤差會顯現的表面。差距也有一部分來自參考點雲本身是用參考姿態建立的。
- **目前的差距在哪：** 產生點的視角看得到約 99% 的 LiDAR 表面，剩下的差距在每個視角本身：
  - 低紋理表面卡在紋理門檻：9F8040 有 48% 的格點被排除，近距離為 31%。
  - 多視角檢查在參考姿態下保留 19–21% 的照片一致像素，相機模式姿態下為 10–16%。

### 預設值的選擇

兩份掃描都做了參數比較，指令見[驗證](#驗證)。以下為參考姿態下的「5 cm 完整度／超過 10 cm 的點／P90 準確度」。

| 變化（與預設的差異） | 7F2187 | 9F8040 |
| --- | --- | --- |
| **預設**（48 視角、紋理 0.012、唯一性 0.05） | 36.9%／2.0%／4.18 cm | 17.3%／1.6%／5.27 cm |
| 紋理門檻 0.02 | 33.4%／1.8%／4.25 cm | 10.1%／3.1%／6.62 cm |
| 不做唯一性檢查 | 39.8%／2.5%／4.63 cm | 19.7%／2.0%／5.48 cm |
| 72 個視角 | 43.9%／2.2%／4.31 cm | 23.3%／2.5%／5.50 cm |
| 96 個視角 | 49.0%／2.8%／5.14 cm | 29.0%／2.8%／5.82 cm |

- **為何選這組預設：**
  - 降低紋理門檻在素牆較多的房間裡，同時提高完整度與準確度。
  - 唯一性檢查少了 3 個百分點的完整度，換到更少離群點，且不會出現重複紋理的鬼影。
  - 視角越多完整度越高，但離群點與耗時也增加：相機模式姿態下，72 個視角讓 7F2187 超過 10 cm 的比例由 7.7% 升到 12.1%，96 個視角耗時是 2.3 倍。
- **先前測過但不採用的設定**（紋理 0.02、無唯一性檢查），都幫助不大：
  - 深度容差 1% → 2%：9F8040 的 5 cm 完整度 11.3 → 16.1%，但超過 10 cm 3.6 → 5.2%。
  - 只要求 1 個視角一致：21.9% 與 7.4%。
  - 6 個來源：13.0% 與 4.9%。
  - 448 px 影像：12.2% 與 3.8%，慢 1.6 倍。
  - 匹配成本 0.45：11.6% 與 3.8%。

**`--all-frames`（7F2187，先前的匹配器）。** 改用全部 399 個已存影格（可用 280 個）而非模擬的 277 個，姿態差異幾乎不變（RPE 1 m 為 1.3 cm、ATE RMSE 1.3 cm）。使用參考姿態的 MVS 點數增加：1,601 點（原為 1,129），5 cm 完整度 14.8%（原為 11.6%）。使用相機模式姿態時仍為 1,003 點與 7.4%（原為 995 點與 7.6%）。準確度中位數大致不變（1.26 與 2.44 cm）。

**參考點間距。** 預設 2 cm 融合 voxel 讓 LiDAR 點間距中位數為 1.3 cm，與準確度數值相差不遠。改用 `--reference-voxel 0.01` 時，間距為 0.73 cm（7F2187，714,180 點）與 0.81 cm（9F8040，已達 2,000,000 點上限），準確度中位數降為 0.87 vs 2.25 cm（7F2187）與 2.21 vs 3.04 cm（9F8040），完整度變化不到 2 個百分點。因此 1 cm 等級的準確度差異，有一部分取決於參考點雲的密度，而不只是 MVS。

### 限制

- 這些掃描的 ARKit 追蹤是在開啟 LiDAR 下進行。Apple 未說明視覺慣性里程計是否使用 LiDAR，因此沒有 LiDAR 或關閉 LiDAR 的手機，漂移可能比這些姿態更大。
- 參考來自 App 的 LiDAR 流程（BA + 照片對齊檢查，再做深度融合），不是真值。
  - 參考 BA 以同一組 ARKit 姿態為初值並向其正則化，它觀測不到的誤差可能也不會出現在差異中。
  - 使用參考姿態的 MVS 是與同一組姿態建立的點雲比較，因此較占優勢。
- 即時的 ARKit 稀疏特徵點不會存檔，所以只評分 MVS 點。App 的預覽點雲在 RGB 表面以外還保留已驗證的稀疏點。
- 抽幀只是近似 RGB 快門（`SmartShutter` 的 `cameraOnly`）：沒有依深度調整的 4–5 cm 平移門檻、3° 旋轉觸發，也沒有特徵與姿態連續性檢查，而且只能從 LiDAR 快門已存的影格中挑選。模糊複核依據的 `estimatedBlurPx` 是用 LiDAR 深度計算的；即時相機模式則使用特徵深度或 0.5 m。
- 準確度採點對點距離，其中包含參考點間距（見上文）；完整度也取決於參考點密度。
- 候選點雲在其自身世界座標中評分，不做對齊；姿態則同時提供原始與對齊後的比較。若候選姿態的規範（gauge）整體移動，例如未固定的純 RGB BA，原始數值與 MVS 分數都會把這個位移算進去。
- 結果只涵蓋同一台裝置的兩次掃描，MVS 預設值也是用這兩份掃描選的。

## 驗證

```sh
swiftc -O -module-cache-path /tmp/fable-swift-cache \
  arkit-3dgs-scanner/Capture/{Localization,Models,BlurFilter,CaptureConfig,Utils,SmartShutter,DepthSampleFilter,RefusionEngine,SurfaceTSDF,CameraOnlyGeometry,SparseLandmarkFilter}.swift \
  tools/test_camera_only_accuracy.swift -o /tmp/fable-camera-only-test
/tmp/fable-camera-only-test
```

RGB 重建回歸指令：

```sh
swiftc -O -module-cache-path /tmp/fable-swift-cache \
  arkit-3dgs-scanner/Capture/{Localization,Models,BlurFilter,CaptureConfig,DepthSampleFilter,RefusionEngine,SurfaceTSDF,RGBStereoMatcher,RGBReconstructionEngine}.swift \
  tools/test_rgb_reconstruction.swift -o /tmp/fable-rgb-test
/tmp/fable-rgb-test
```

LiDAR 參考重播的指標回歸：

```sh
bash tools/test_camera_only_replay.sh   # 重播指標 32 項、相機模式 BA 11 項，並建置兩個重播工具

# 對 LiDAR 掃描做相機模式姿態精修再評分（原始檔清單見腳本）
/tmp/refine_camera_only SCAN /tmp/refined.jsonl
/tmp/replay_camera_only SCAN WORK_DIR --reference /tmp/ref.jsonl --candidate /tmp/refined.jsonl

# MVS 參數比較：以環境變數覆寫重播工具的設定（會記錄在報告中）
MVS_STD=0.02 MVS_REFS=72 /tmp/replay_camera_only SCAN WORK_DIR --reference /tmp/ref.jsonl
```

`MVS_DIM`、`MVS_REFS`、`MVS_STRIDE`、`MVS_SOURCES`、`MVS_ITERS`、`MVS_STD`、`MVS_COST`、`MVS_VIEWS`、`MVS_RATIO`、`MVS_UNIQUE` 會在單次重播覆寫對應的 `rgb*` 設定。

重播指標檢查涵蓋下列合成案例：
- 相同、剛體移動（30° 與 150°）、放大 2% 與每公尺漂移 1 cm 的軌跡。
- Jacobi 特徵值求解，以及雜湊網格搜尋與暴力搜尋的比對。
- 偏移 1 cm 的平面（準確度、半平面在 2／5／10 cm 的完整度、遮罩）、遠處離群點與點間距。
- 可見遮罩：可見、被遮擋、容差、超出影像、低信心與過近。
- 快門抽幀。

相機模式 BA 測試渲染有紋理的房間角落，沿 30 個關鍵幀側向移動，檢查：
- 不帶深度的 track，以及它與真實幾何的次像素一致性；
- 保留集確認的修正，且位移誤差不變大；
- 正確輸入姿態不被改動，重複執行結果相同；
- 白牆不產生 track；
- 俯仰錯誤的影格被極線檢查排除；
- 讀不到的影格、取消，以及報告的編碼與解碼。

RGB 重建測試共 33 項；建置仍有既有融合程式 UnsafeMutableBufferPointer 的 Sendable 警告。

RGB 測試直接渲染已知 1.5 m 的紋理平面，再給不同相機位移／轉角及亮度，涵蓋：
- 斜面、純旋轉、白牆、單方向與雙方向重複紋理（由紋理門檻與唯一性檢查排除）；
- 第三視角遮擋與不一致幾何；
- 平行深度圖每次執行結果完全相同；
- 來源選擇會跳過反方向與近乎重複的相機，並遵守來源數上限；
- 報告的像素漏斗；
- JPEG 壓縮與縮圖、影像方向、voxel 合併、稀疏覆蓋補充與視角上限；
- 缺檔、內參尺寸不符、畸形姿態、模糊判定、重複影格及舊 metadata 相容性。

合成平面測試要求中位深度誤差 < 1.5 cm、P95 < 4 cm，斜面中位平面殘差 < 2.5 cm；PatchMatch 在理想平面為 1.7 mm、斜面為 1.6 mm。這是理想、已知姿態場景的回歸門檻，不能推論真機精度。

26 項合成回歸檢查涵蓋投影座標／可見性、特徵分布、近側距離與平移模糊、重複 ID、無基線／足夠基線、漂移與過期觀測、epoch 隔離、容量、近距離快門、LiDAR 旋轉行為、姿態突跳及恢復。這些檢查驗證接收／拒絕行為，不是現場幾何精度測量。

真機比較時固定光線、相機設定及路徑，使用有紋理的平面與已知尺寸物件；分別測試側向掃描、原地旋轉、白牆、背景切換／重新定位。記錄接受幀數、點數、已知尺寸誤差與表面厚度，避免把更多點視為更準。尚未完成此輪真機 A/B 與 UI 操作驗證。

## API 依據

Apple 將 [rawFeaturePoints](https://developer.apple.com/documentation/arkit/arframe/rawfeaturepoints) 定義為追蹤使用的中間分析結果，且不保證跨影格的點數與排列穩定。[ARPointCloud.identifiers](https://developer.apple.com/documentation/arkit/arpointcloud/identifiers) 提供點的唯一識別碼；本實作依 ID 關聯觀測，不依陣列位置，對消失、更新與追蹤中斷採保守處理。

已知相機姿態可作為多視角重建的輸入，見 [COLMAP 官方 FAQ](https://colmap.github.io/faq.html#reconstruct-sparse-dense-model-from-known-camera-poses)。本實作是獨立的手機端保守區塊匹配器，未整合 COLMAP／PatchMatch。
